@model List<Web_CuaHangCafe.Models.TbNhomSanPham>
@{
    var role = Context.Session.GetString("Role");
}
<div class="card">
    <div class="card-header">Danh Sách Tin Tức</div>
    <div class="card-body">
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Tên Nhóm Sản Phẩm</th>
                    @if (role == "Admin")
                    {
                        <th>Hành động</th>
                    }
                </tr>
            </thead>
            <tbody>
                @foreach (var nsp in Model)
                {
                    <tr id="row-@nsp.MaNhomSp">
                        <td>@nsp.MaNhomSp</td>
                        <td>@nsp.TenNhomSp</td>
                       
                        @if (role == "Admin")
                        {
                            <td>
                                <button class="btn btn-sm btn-warning" onclick="editNhomSP(@nsp.MaNhomSp)">Sửa</button>
                                <button class="btn btn-sm btn-danger" onclick="deleteNhomSP(@nsp.MaNhomSp)">Xóa</button>
                            </td>
                        }
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

<script>
    // Hàm gọi load partial view cho form sửa, truyền id tin tức
    async function editNhomSP(id) {
        try {
            const response = await fetch(`/NhomSPTest/PartialEdit?id=${id}`);
            if (!response.ok) throw new Error(response.statusText);
            const html = await response.text();
            document.getElementById("mainContainer").innerHTML = html;
            attachEditFormHandler();
        } catch(error) {
            console.error("Lỗi load form sửa:", error);
            alert("Không tải được form sửa!");
        }
    }

    // Hàm gán sự kiện submit cho form sửa
    function attachEditFormHandler() {
        const editForm = document.getElementById("editForm");
        if (editForm) {
            editForm.addEventListener("submit", async function(e) {
                e.preventDefault();
                const formData = new FormData(editForm);
                let data = {};
                formData.forEach((value, key) => { data[key] = value });
                const tokenElem = document.querySelector('input[name="__RequestVerificationToken"]');
                const token = tokenElem ? tokenElem.value : "";
                try {
                    const response = await fetch("/NhomSPTest/Edit", {
                        method: "POST",
                        headers: {
                            "Content-Type": "application/json",
                            "RequestVerificationToken": token
                        },
                        body: JSON.stringify(data)
                    });
                    const result = await response.json();
                    alert(result.message);
                    if(result.success) {
                        loadContent("list");
                    }
                } catch(error) {
                    console.error("Lỗi sửa:", error);
                    alert("Có lỗi khi cập nhật tin tức!");
                }
            });
        } else {
            console.error("Không tìm thấy form editForm.");
        }
    }

    // Hàm xử lý xoá tin tức sử dụng Fetch API
    async function deleteNhomSP(id) {
        if (!confirm("Bạn có chắc muốn xóa tin tức này?")) return;
        const tokenElem = document.querySelector('input[name="__RequestVerificationToken"]');
        const token = tokenElem ? tokenElem.value : "";
        try {
            const response = await fetch("/NhomSPTest/Delete", {
                method: "POST",
                headers: {
                    "Content-Type": "application/json",
                    "RequestVerificationToken": token
                },
                body: JSON.stringify(id)
            });
            const result = await response.json();
            alert(result.message);
            if(result.success) {
                loadContent("list");
            }
        } catch(error) {
            console.error("Lỗi xoá:", error);
            alert("Có lỗi khi xóa tin tức!");
        }
    }
</script>
